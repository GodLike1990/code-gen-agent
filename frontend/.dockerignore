# Dependencies — installed fresh inside the container
node_modules/

# Build output — generated by `npm run build` inside the container
dist/
.vite/

# TypeScript incremental build cache
tsconfig.tsbuildinfo

# Local env overrides
*.local
.env.local
.env.*.local

# Editor / OS
.DS_Store
*.swp
